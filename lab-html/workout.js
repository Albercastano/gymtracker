let A=null,timer=null,execTimer=null;function currentRoutine(){return A?rt(A.session.routineId):null}function currentItem(){let r=currentRoutine();return r?.items?.[A.idx]||null}function currentSet(){let it=currentItem();return it?(A.done[it.id]||0)+1:1}function startRoutine(rid){let r=rt(rid);if(!r||!r.items?.length){alert("Rutina vacía");return}A={session:{id:id(),date:today(),routineId:rid,start:now(),note:""},idx:0,done:{},sets:[],pendingNextExercise:false};saveActiveWorkout(A);GT.show("workout");renderWorkout()}function bestSetForExercise(eid){let ss=S.sets.filter(x=>x.exerciseId===eid);if(!ss.length)return null;return ss.slice().sort((a,b)=>((+b.weight||0)*(+b.reps||0))-((+a.weight||0)*(+a.reps||0)))[0]}function lastSetsForExercise(eid){for(let sess of S.sessions.slice().reverse()){let ss=S.sets.filter(x=>x.sessionId===sess.id&&x.exerciseId===eid);if(ss.length)return ss}return[]}function lastSessionHtml(eid,mode){let ss=lastSetsForExercise(eid);return ss.length?ss.map(s=>mode==="time"?`${s.reps}s`:`${s.weight||0}kg × ${s.reps||0}`).join("<br>"):"Sin datos"}function suggestionForItem(it){let last=lastSetsForExercise(it.exerciseId);if(!last.length)return"Sin datos";let target=it.repsPlan?.[0]||0,ok=last.every(s=>(+s.reps||0)>=target);if(ok&&it.mode==="reps")return`Puedes probar +${S.settings.inc} kg`;if(ok&&it.mode==="time")return"Puedes subir segundos";return"Mantener"}function recordSet(reps,weight){let it=currentItem(),e=ex(it.exerciseId),set=currentSet();A.sets.push({id:id(),sessionId:A.session.id,exerciseId:e.id,setNumber:set,reps:+reps||0,weight:+weight||0,mode:it.mode,order:A.idx,pr:false});if(weight>0){e.lastWeight=+weight;if(!e.defaultWeight)e.defaultWeight=+weight}A.done[it.id]=set;saveActiveWorkout(A);if(set>=(+it.sets||1)){if(A.idx>=currentRoutine().items.length-1){endWorkout();return}A.pendingNextExercise=true;saveActiveWorkout(A);startRest(it.rest||S.settings.rest);return}startRest(it.rest||S.settings.rest)}function finishSet(){let it=currentItem(),set=currentSet();if(it.mode==="time"){startExecutionTimer(it.repsPlan?.[set-1]||it.repsPlan?.[0]||ex(it.exerciseId)?.defaultTime||S.settings.defaultTime);return}let target=it.mode==="failure"?0:(it.repsPlan?.[set-1]||it.repsPlan?.[0]||0),reps=it.mode==="failure"?+(prompt("Reps realizadas","0")||0):+(prompt("Reps realizadas",String(target))||target),e=ex(it.exerciseId),w0=it.weight||e.lastWeight||e.defaultWeight||0,weight=+(prompt("Peso usado",String(w0))||0);recordSet(reps,weight)}function startExecutionTimer(sec){let left=+sec||20,e=ex(currentItem().exerciseId);clearInterval(execTimer);document.getElementById("workout").innerHTML=`<div class="timer"><div class="casio"><div class="lcd"><div>EJERCICIO</div><div id="execTime" class="lcdTime">${fmt(left)}</div><div>${e.name}</div><div class="row"><button class="primary" onclick="completeTimedSet(${left})">Completar</button><button onclick="stopTimedSet()">Parar</button></div></div></div></div>`;execTimer=setInterval(()=>{left--;let el=document.getElementById("execTime");if(el)el.textContent=fmt(left);if(left<=0){clearInterval(execTimer);beep();buzz();completeTimedSet(sec)}},1000)}function completeTimedSet(t){let done=+(prompt("Segundos realizados",String(t))||t);recordSet(done,0)}function stopTimedSet(){let done=+(prompt("Segundos realizados","0")||0);recordSet(done,0)}function startRest(sec){let left=+sec||120;clearInterval(timer);document.getElementById("workout").innerHTML=`<div class="timer"><div class="casio"><div class="lcd"><div>DESCANSO</div><div id="lcdTime" class="lcdTime">${fmt(left)}</div><div>Siguiente: ${nextName()}</div><button class="primary" onclick="skipRest()">Saltar</button></div></div></div>`;timer=setInterval(()=>{left--;let el=document.getElementById("lcdTime");if(el)el.textContent=fmt(left);if(left<=0){beep();buzz();skipRest()}},1000)}function skipRest(){clearInterval(timer);if(A?.pendingNextExercise){A.pendingNextExercise=false;A.idx++;saveActiveWorkout(A)}renderWorkout()}function fmt(s){s=Math.max(0,+s||0);return String(Math.floor(s/60)).padStart(2,"0")+":"+String(s%60).padStart(2,"0")}function nextName(){if(A?.pendingNextExercise){let n=currentRoutine().items[A.idx+1];return n?ex(n.exerciseId)?.name:"Fin"}let it=currentItem();return it?ex(it.exerciseId)?.name:"Fin"}function coachSummary(sid){let sess=S.sessions.find(x=>x.id===sid),r=rt(sess.routineId),sets=S.sets.filter(x=>x.sessionId===sid),vol=sets.reduce((a,x)=>a+(+x.weight||0)*(+x.reps||0),0),out=`${r?.name||"Rutina"} · ${sess?.date||today()}\n\nSeries: ${sets.length}\nVolumen: ${Math.round(vol)} kg\n\n`,by={};sets.forEach(x=>(by[x.exerciseId]=by[x.exerciseId]||[]).push(x));Object.entries(by).forEach(([eid,ss])=>{out+=`${ex(eid)?.name||"Ejercicio"}\n`;ss.forEach(x=>out+=x.mode==="time"?`${x.reps}s\n`:`${x.weight||0}kg x ${x.reps||0}${x.pr?" 🏆":""}\n`);out+="\n"});return out}function endWorkout(){if(!A)return;A.session.end=now();let prev=S.sets.slice(),prs=[],grouped={};A.sets.forEach(st=>(grouped[st.exerciseId]=grouped[st.exerciseId]||[]).push(st));Object.entries(grouped).forEach(([eid,ss])=>{let old=Math.max(0,...prev.filter(x=>x.exerciseId===eid).map(x=>(+x.weight||0)*(+x.reps||0))),best=ss.slice().sort((a,b)=>((+b.weight||0)*(+b.reps||0))-((+a.weight||0)*(+a.reps||0)))[0],nw=(+best.weight||0)*(+best.reps||0);if(nw>old&&nw>0){best.pr=true;prs.push(ex(eid)?.name)}});S.sessions.push(A.session);S.sets.push(...A.sets);S.plan[today()]={date:today(),routineId:A.session.routineId,status:"done"};save();postWorkoutBackup();clearActiveWorkout();let sid=A.session.id,summary=coachSummary(sid);if(prs.length){summary+="\n🏆 PR por ejercicio: "+prs.join(", ")+"\n";beep();buzz(220)}A=null;GT.show("home");GT.render();setTimeout(()=>showFinalSummary(summary,sid),100)}function showFinalSummary(txt,sid){let sets=S.sets.filter(x=>x.sessionId===sid),vol=sets.reduce((a,x)=>a+(+x.weight||0)*(+x.reps||0),0),prs=sets.filter(x=>x.pr).length,box=document.getElementById("summaryBox");box.innerHTML=`<h2>Resumen final</h2><div class="statGrid"><div class="metric"><span>Series</span><b>${sets.length}</b></div><div class="metric"><span>Volumen</span><b>${Math.round(vol)}</b></div><div class="metric"><span>PR</span><b>${prs}</b></div></div><div class="coachReportBox">${txt}</div><div class="row"><button class="primary" onclick="navigator.clipboard?.writeText(document.querySelector('#summaryBox .coachReportBox').innerText);alert('Copiado')">Copiar</button><button onclick="GT.show('stats');document.getElementById('summaryOverlay').classList.remove('on')">Editar historial</button><button onclick="document.getElementById('summaryOverlay').classList.remove('on')">Cerrar</button></div>`;document.getElementById("summaryOverlay").classList.add("on")}function pauseWorkoutToHome(){if(A)saveActiveWorkout(A);GT.show("home");GT.render()}function resumeActiveWorkout(){let old=getActiveWorkout();if(!old)return false;A=old;GT.show("workout");renderWorkout();return true}function discardActiveWorkout(){if(confirm("¿Descartar entrenamiento en curso?")){clearActiveWorkout();GT.render()}}function renderWorkout(){let r=currentRoutine(),it=currentItem();if(!A||!r||!it){document.getElementById("workout").innerHTML=`<div class="card"><h2>No hay entrenamiento activo</h2><button class="primary" onclick="GT.show('home')">Volver</button></div>`;return}let e=ex(it.exerciseId),set=currentSet(),target=it.mode==="failure"?"F":(it.repsPlan?.[set-1]||it.repsPlan?.[0]||0),best=bestSetForExercise(it.exerciseId);document.getElementById("workout").innerHTML=`<div class="card focus"><div><div class="muted">${r.name}</div><div class="focusTitle">${e.name}</div><div class="timeBadge">${it.mode==="time"?"Tiempo":it.mode==="failure"?"Fallo":"Reps"} ${it.superset?`<span class="supersetBadge">SS ${it.superset}</span>`:""}</div><div class="lastBox"><strong>Última sesión</strong><br>${lastSessionHtml(it.exerciseId,it.mode)}<br><br><strong>Mejor marca</strong><br>${best?(best.weight+"kg × "+best.reps):"Sin PR"}<br><br><strong>Sugerencia</strong><br>${suggestionForItem(it)}<br><br><strong>Descanso</strong><br>${fmt(it.rest||S.settings.rest)}</div></div><div class="focusStats"><div class="focusStat"><span>Serie</span><b>${set}/${it.sets}</b></div><div class="focusStat"><span>Objetivo</span><b>${target}</b></div><div class="focusStat"><span>Peso</span><b>${it.mode==="time"?"—":(it.weight||e.lastWeight||e.defaultWeight||0)}</b></div></div><button class="primary wall bigAction" onclick="finishSet()">${it.mode==="time"?"Iniciar crono":"Fin serie"}</button><div class="row"><button onclick="A.idx=Math.max(0,A.idx-1);saveActiveWorkout(A);renderWorkout()">Anterior</button><button onclick="A.idx=Math.min(r.items.length-1,A.idx+1);saveActiveWorkout(A);renderWorkout()">Siguiente</button><button onclick="pauseWorkoutToHome()">Salir sin terminar</button><button class="danger" onclick="endWorkout()">Finalizar</button></div></div>`}window.addEventListener("beforeunload",()=>{try{if(A)saveActiveWorkout(A)}catch(e){}})