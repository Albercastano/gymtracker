"use strict";
(function(){
  const VERSION="0.1.0",ATTR="data-forge-action";
  const esc=v=>String(v??"").replace(/[&<>\"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[c]));
  const num=v=>Number.isFinite(Number(v))?Number(v):0;
  const fmt=value=>{const s=Math.max(0,Math.round(num(value))),m=Math.floor(s/60);return `${String(m).padStart(2,"0")}:${String(s%60).padStart(2,"0")}`};
  const action=(name,label,payload="")=>`${ATTR}="${esc(name)}" aria-label="${esc(label)}"${payload?` data-forge-payload="${esc(payload)}"`:""}`;
  const instruments=window.PhoenixForgeInstruments;
  instruments.register("precision-timer-header",{render:({data})=>`<header class="phoenix-timer__header" data-forge-instrument="precision-timer-header"><div class="phoenix-timer__brand"><img src="icon-192.png" alt=""><div><b>PHOENIX</b><span>GYMTRACKER</span></div></div><div class="phoenix-timer__title"><span>DESCANSO</span><i></i></div><div class="phoenix-timer__series"><div><small>SERIE ACTUAL</small><b>${num(data.currentSeries)}/${num(data.totalSets)}</b></div><span aria-hidden="true">→</span><div><small>SIGUIENTE</small><b>${num(data.nextSeries)}/${num(data.totalSets)}</b><em>${esc(data.exerciseName)}</em></div></div></header>`});
  instruments.register("precision-timer-core",{render:({data})=>`<div class="phoenix-timer__instrument" data-forge-instrument="precision-timer-core"><div class="phoenix-timer__screw phoenix-timer__screw--tl"></div><div class="phoenix-timer__screw phoenix-timer__screw--tr"></div><div class="phoenix-timer__screw phoenix-timer__screw--bl"></div><div class="phoenix-timer__screw phoenix-timer__screw--br"></div><div class="phoenix-timer__plate">PHOENIX TIMER</div><div id="phoenixTimerRing" class="phoenix-timer__ring" style="--progress:${Math.max(0,Math.min(1,num(data.left)/Math.max(1,num(data.total))))}"><svg class="phoenix-timer__svg" viewBox="0 0 120 120" aria-hidden="true"><defs><linearGradient id="apexTimerGradient" x1="0" y1="0" x2="1" y2="1"><stop offset="0%" stop-color="#00d9ff"></stop><stop offset="56%" stop-color="#00d9ff"></stop><stop offset="100%" stop-color="#00e676"></stop></linearGradient></defs><circle class="phoenix-timer__svg-track" cx="60" cy="60" r="53" pathLength="100"></circle><circle id="phoenixTimerArc" class="phoenix-timer__svg-arc" cx="60" cy="60" r="53" pathLength="100"></circle><circle class="phoenix-timer__svg-inner" cx="60" cy="60" r="45" pathLength="100"></circle></svg><div class="phoenix-timer__ticks" aria-hidden="true"></div><div class="phoenix-timer__core"><span class="phoenix-timer__hourglass" aria-hidden="true">◷</span><div id="restTime" class="phoenix-timer__time" role="timer" aria-live="off">${fmt(data.left)}</div><div class="phoenix-timer__next">SIGUIENTE SERIE</div><div id="phoenixTimerState" class="phoenix-timer__state">${data.paused?"PAUSADO":"DESCANSO"}</div><div class="phoenix-timer__telemetry"><span id="phoenixTimerPercent">${Math.round(Math.max(0,Math.min(1,num(data.left)/Math.max(1,num(data.total))))*100)}%</span><i></i><span id="phoenixTimerTotal">${fmt(data.total)} TOTAL</span></div><span id="phoenixTimerLive" class="sr-only" aria-live="polite"></span></div><div class="phoenix-timer__spark" aria-hidden="true"></div></div><div class="phoenix-timer__status"><span>●</span><b id="phoenixTimerPulse">PRECISIÓN FORGED</b></div></div>`});
  instruments.register("precision-timer-controls",{render:({data})=>`<div class="phoenix-timer__controls" data-forge-instrument="precision-timer-controls"><button type="button" ${action("adjust-rest","Restar treinta segundos",JSON.stringify({delta:-30}))}><b>−30</b><span>SEGUNDOS</span></button><button type="button" id="restPauseButton" class="phoenix-timer__pause" ${action(data.paused?"resume-timer":"pause-timer",data.paused?"Reanudar descanso":"Pausar descanso")}><b>${data.paused?"▶":"Ⅱ"}</b><span>${data.paused?"SEGUIR":"PAUSA"}</span></button><button type="button" ${action("adjust-rest","Añadir treinta segundos",JSON.stringify({delta:30}))}><b>+30</b><span>SEGUNDOS</span></button></div>`});
  instruments.register("precision-timer-skip",{render:()=>`<button class="phoenix-timer__skip" ${action("skip-rest","Saltar descanso")} data-forge-instrument="precision-timer-skip"><span>»</span> SALTAR DESCANSO</button>`});
  instruments.register("precision-timer-settings",{render:({data})=>`<footer class="phoenix-timer__footer" data-forge-instrument="precision-timer-settings"><button type="button" ${action("toggle-timer-sound","Alternar sonido")}><span>◖))</span><div><small>SONIDO</small><b id="timerSoundStatus">${data.sound?"ACTIVADO":"DESACTIVADO"}</b><em id="timerAudioReady">${data.sound?(data.audioReady?"LISTO":"TOCA PARA ACTIVAR"):"OFF"}</em></div></button><button type="button" ${action("toggle-timer-vibration","Alternar vibración")}><span>▣</span><div><small>VIBRACIÓN</small><b id="timerVibrationStatus">${data.vibration?"ACTIVADA":"DESACTIVADA"}</b></div></button></footer>`});
  function payload(node){try{return JSON.parse(node.getAttribute("data-forge-payload")||"{}")}catch(_){return {}}}
  function bind(target){if(target.dataset.forgeRestActionsBound==="1")return;target.dataset.forgeRestActionsBound="1";target.addEventListener("click",async event=>{const node=event.target.closest(`[${ATTR}]`);if(!node||!target.contains(node))return;event.preventDefault();try{await window.PhoenixForgeActionBus.dispatch(node.getAttribute(ATTR),payload(node))}catch(error){console.warn("Forge Rest action",error)}})}
  function render({target,shape,snapshot,context={}}){if(!target||!shape?.screens?.rest||snapshot?.screen!=="rest")throw new Error("Rest Renderer: contrato incompleto");const screen=shape.screens.rest;const html=screen.slots.slice().sort((a,b)=>(a.order||0)-(b.order||0)).map(slot=>instruments.render(slot.instrument,{slot,data:snapshot.components?.[slot.component]||{},snapshot,context})).join("");target.innerHTML=`<div class="focus phoenix-timer-screen"><section id="phoenixTimer" class="phoenix-timer" aria-label="Phoenix Timer de descanso" data-forge-renderer="rest">${html}</section></div>`;bind(target);return Object.freeze({ok:true,screen:"rest",shape:context.shapeId||shape.id,renderer:VERSION,instruments:screen.slots.map(slot=>slot.instrument)})}
  window.PhoenixForgeRestRenderer=Object.freeze({version:VERSION,render});
})();
