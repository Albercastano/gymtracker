# Phoenix Sync Provider · contrato reservado

Estado actual: **desactivado** (`NoSyncProvider`).

Una implementación futura deberá ofrecer únicamente: autenticación explícita, copia cifrada, descarga, lista de dispositivos, resolución de conflictos y borrado remoto. La app local seguirá funcionando sin suscripción ni red.
